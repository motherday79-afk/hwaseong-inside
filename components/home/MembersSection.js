import { members } from '../../data/members';
export default function MembersSection(){return <section className="webSection webMembers" id="members"><div className="webWrap"><div className="webTitleRow"><div><h2>40인 멤버</h2><p>각 분야에서 활약하는 화성인 멤버들을 소개합니다.</p></div><a href="/members">전체 멤버 보기 →</a></div><div className="webMemberGrid">{members.slice(10,30).map(m=><a className="webMember" href={`/members/${m.id}`} key={m.id}><img src={m.image} alt={`${m.name} 프로필`}/><strong>{m.name}</strong><small>{m.job}</small></a>)}</div><div className="webMore"><a href="/members">더 보기 ↓</a></div></div></section>}
