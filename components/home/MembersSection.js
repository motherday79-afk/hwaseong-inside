'use client';
import { useSiteAdminData } from '../SiteAdminData';
function Portrait({m}){const src=m.profileImage||m.image;return src?<img src={src} alt={`${m.name} 프로필`}/>:<div className="v7MiniAvatarFallback">{(m.name||'?').slice(0,1)}</div>}
export default function MembersSection(){const {members}=useSiteAdminData();const list=members.slice(10,30);return <section className="v7Section v7Members" id="members"><div className="v7Wrap"><div className="v7TitleRow"><div><h2>40인 멤버</h2><p>각 분야에서 활약하는 화성인 멤버들을 소개합니다.</p></div><a href="/members">전체 멤버 보기 <b>›</b></a></div><div className="v7MemberGrid">{list.map(m=><a className="v7Member" href={`/members/${m.id}`} key={m.id}><div className="v7MemberPhoto"><Portrait m={m}/></div><strong>{m.name}</strong><small>{m.job}</small></a>)}</div><div className="v7More"><a href="/members">더 보기⌄</a></div></div></section>}
