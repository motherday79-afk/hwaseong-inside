'use client';
import { useSiteAdminData } from '../SiteAdminData';
const icons=['📣','🏆','📅'];
export default function NewsSection(){const {data}=useSiteAdminData();const news=(data.news||[]).filter(n=>n.published).slice(0,3);return <section className="webSection webNews"><div className="webWrap"><div className="webTitleRow"><h2>공지 & 뉴스</h2><a href="/notice-news">전체보기 →</a></div><div className="webNewsGrid">{news.map((n,i)=><a className="webNewsCard" href={`/notice-news/${n.id}`} key={n.id}><div className={`webNewsVisual tone${i+1}`} style={n.coverUrl?{backgroundImage:`url(${n.coverUrl})`,backgroundSize:'cover',backgroundPosition:'center'}:{}}><span>{n.coverUrl?'':icons[i]||'📰'}</span><small>{n.category}</small></div><div className="webNewsBody"><strong>{n.title}</strong><p>{n.excerpt}</p><time>{n.date}</time></div></a>)}</div></div></section>}
