'use client';
import { useSiteAdminData } from '../SiteAdminData';
import BrandIcon from '../BrandIcon';
const glyphs=['megaphone','calendar','sparkles'];
export default function NewsSection(){const {data}=useSiteAdminData();const news=(data.news||[]).filter(n=>n.published).slice(0,3);return <section className="v7Section v7News"><div className="v7Wrap"><div className="v7TitleRow"><h2><span className="v7TitleIcon"><BrandIcon name="megaphone" size={22}/></span> 공지 & 뉴스</h2><a href="/notice-news">전체보기 <BrandIcon name="arrowRight" size={14}/></a></div><div className="v7NewsGrid">{news.map((n,i)=><a className="v7NewsCard" href={`/notice-news/${n.id}`} key={n.id}><div className={`v7NewsVisual tone${i+1}`} style={n.coverUrl?{backgroundImage:`url(${n.coverUrl})`}:{}}>{!n.coverUrl&&<BrandIcon name={glyphs[i]||'megaphone'} size={46} className="v7NewsGlyph" strokeWidth={1.45}/>}</div><div className="v7NewsBody"><strong>{n.title}</strong><p>{n.excerpt}</p><time>{n.date}</time></div></a>)}</div></div></section>}
