import { IMAGE_SPECS } from '../../../data/adminDefaults';
import { PageTitle, Card, Spec } from '../../../components/admin/AdminUI';
export default function MediaGuide(){return <div className="adminPage"><PageTitle title="이미지 등록 규격" desc="등록 단계에서 원본 규격을 통일해 PC·모바일·상세페이지에서 해상도가 깨지는 문제를 예방합니다."/><Card title="권장 이미지 규격" desc="표시 크기가 아니라 업로드 원본 기준입니다. 웹에서는 원본을 자동 크롭/리사이즈합니다."><div className="adminSpecGrid">{Object.entries(IMAGE_SPECS).map(([k,s])=><div className="adminSpecCard" key={k}><div className="adminSpecIcon">▧</div><h3>{s.label}</h3><Spec spec={s}/></div>)}</div></Card><Card title="프로필 이미지 자동 연동 원칙"><div className="adminFlow"><span>회원가입</span><i>→</i><span>1200×1200 원본 등록</span><i>→</i><span>회원 DB 저장</span><i>→</i><span>Top10 / 40인 / 상세 자동 사용</span></div><p className="adminNote">같은 사람의 이미지를 페이지마다 별도로 다시 만들지 않습니다. 프로필 원본 하나를 기준으로 CSS의 <code>object-fit: cover</code>와 위치값만 다르게 적용하는 구조가 최종 목표입니다.</p></Card></div>}
